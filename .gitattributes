# Normalize all text files to LF on commit (works on Windows + Linux)
* text=auto eol=lf

# Explicitly binary — never touch line endings
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
*.woff binary
